<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.food" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Food"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".Scrolling_item_Activity"
            android:label="@string/title_activity_scrolling_item_"
            android:theme="@style/Theme.Food.NoActionBar" />
        <activity android:name=".DashBoardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".InductoryActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>


        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Refresh_Activity" />
        <activity android:name=".Progress_Activity" />
        <activity android:name=".DrawlayoutActivity">

        </activity>
    </application>

</manifest>